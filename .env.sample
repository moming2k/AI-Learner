# =============================================================================
# AI Learning Wiki - Environment Configuration Sample
# =============================================================================
# Copy this file to .env.local and fill in your values
# DO NOT commit .env.local to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# OpenAI API Configuration (REQUIRED)
# -----------------------------------------------------------------------------
# Get your API key from: https://platform.openai.com/api-keys
# This is required for the AI to generate wiki content
OPENAI_API_KEY=your-openai-api-key-here

# Optional: OpenAI API base URL (default: https://api.openai.com/v1)
# For Azure OpenAI or other OpenAI-compatible providers, update this URL
# OPENAI_API_BASE_URL=https://api.openai.com/v1

# Optional: OpenAI Model (default: gpt-5)
# Supported models: gpt-5, gpt-4o, gpt-4o-mini, gpt-4-turbo-preview, gpt-3.5-turbo
# Note: gpt-5 requires special access from OpenAI
# OPENAI_MODEL=gpt-5
